spring:
  datasource:
    url: jdbc:h2:mem:clouddent_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false

app:
  jwt:
    secret: TestSecretKeyParaCloudDentMinimo32Chars!!
    expiration-ms: 3600000
  cors:
    allowed-origins: http://localhost
